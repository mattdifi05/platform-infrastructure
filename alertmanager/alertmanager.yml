global:
  resolve_timeout: 5m

route:
  receiver: platform-alert-dispatcher
  group_by:
    - alertname
    - service
    - severity
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    - receiver: platform-alert-dispatcher
      matchers:
        - platform_probe="alert-delivery"
      group_wait: 0s
      group_interval: 1m
      repeat_interval: 1h
    - receiver: platform-alert-dispatcher
      matchers:
        - severity="critical"
      repeat_interval: 1h

receivers:
  - name: platform-alert-dispatcher
    webhook_configs:
      - url: http://platform-alert-dispatcher:3000/alerts/prometheus
        send_resolved: true
        max_alerts: 20
        http_config:
          authorization:
            type: Bearer
            credentials_file: /run/secrets/alertmanager_webhook_token

inhibit_rules:
  - source_matchers:
      - severity="critical"
    target_matchers:
      - severity="warning"
    equal:
      - alertname
      - service
      - instance
