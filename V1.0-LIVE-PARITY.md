# V1.0 live parity inventory

Captured on 2026-08-30 before any V1.1 cleanup.

Current gate:

- `V1_0_PARITY = AWAITING_PLATFORM_MAIN`
- `COHORT_1_CLEANUP = NO_GO`

The live runtime is healthy. This gate exists because protected main at the
start of the capture (`b5ff4e879095f6363c88469a1c822a2cce90c877`, tree
`53df992a21a3a652f99b4b37e39b412299d3d056`) did not yet contain every
intentional live source/configuration change.

## Authorities

| Authority | Identity |
| --- | --- |
| Live Compose project | `platform_infra_greenfield` on `192.168.1.164` |
| Live base render | SHA-256 `03eae7431b600cbe192f3fc2ad84e76a4f2503cfba2c50107cc1e0911084a2bd` |
| Effective live render including active overlays | SHA-256 `76bdb47313b34209352ff8039986dea86d4161910af9b7462af657b14c51807a` |
| Candidate direct render | SHA-256 `1b3762adaa71592f80a8ccba1eac6cd8d5e38830623503404fac7d1ed19e896d` |
| Candidate/live service-set comparison | `32/32`, no missing or extra service |
| Application source lock | `config/v1-local-private-source-lock.json` |

The candidate was rendered directly with Docker Compose and the file order in
the source lock. No reconciler, STEP1/STEP2 transaction or fresh-bootstrap
executor was used. The render and comparisons did not replace a container,
network, volume, image or live state.

## Versioned intentional differences

| Surface | Live difference captured for Git | Versioned owner |
| --- | --- | --- |
| Control Center backend | SimpleWebAuthn `app-passkey`, PostgreSQL credential/session persistence, one-day session, direct reauthentication, database/backup/secret operations and Redis-backed cache/operations | platform repository |
| Control Center frontend | current navigation, global Secrets view, per-app database/backup UI, preload/cache behavior, resource metrics and mutation UX | platform repository |
| First configuration | direct first passkey enrollment, minimum one passkey, no required Keycloak administrator handoff | platform repository |
| Host and subdomains | CoreDNS apex/wildcard zone for `platform-infrastructure.com`; exact 12-host map in `APPLICATION_RUNTIME_MAP.md` | platform repository plus external LAN DNS selection |
| Traefik | greenfield edge subnet `.251`, WAF as trusted peer, portal/auth/API routes, wildcard project router and tracked dynamic configuration | platform repository |
| WAF | exact Keycloak authorization-endpoint exclusion for the structured `redirect_uri`; all other CRS targets stay enabled | platform repository |
| Project router | all runtime modules copied into its image; exact allowed upstreams and locked workload metadata | platform repository plus external state lock |
| Compose and networks | current 32-service topology, dedicated app containers, DNS/admin services enabled, application ingress/egress/cache networks and greenfield volume/network names | platform repository |
| Dockerfiles/images | Control Center, project-router, common PHP runtime and reproducible Stexor backend/web/worker Dockerfiles; upstream images remain digest-pinned | platform and Stexor repositories |
| Application configuration | current runtime commands, hosts, build args, service dependencies, source mounts and database mapping | platform repository and owning app repository |
| Application source | exact reviewed source extracted from the live source boundary | three private application repositories below |

## Locked application source

| Repository | Main commit | Tree | Live applications |
| --- | --- | --- | --- |
| `mattdifi05/FeI-Platform` | `e8b891b6d56fac9aed9ba1c5b7866df26bd7fa25` | `38d24216b1681f01d40461ce0eea54fae6214770` | `fiplatform`, `fireport` alias |
| `mattdifi05/Stexor-account` | `60be3769302500777ff557e06284db0300136659` | `0d65cbb9b61cc45788c869a4da19612543205e43` | API, account, UI and workers |
| `mattdifi05/Platform-hosted-applications` | `5646f5f981501a1f140bbbdf6d3254a409c01068` | `0937117c6f7f3bb7851d8fb683693a75c8840707` | anniversary, matthewdifilippo, opstudents, stream, workcalendar |

Fresh remote clones of all three `main` revisions were clean and matched the
commit/tree coordinates above. Dependency directories, build output, `.env`,
uploads, logs and caches were excluded deliberately; none is the sole copy of
application source.

## State, data and secrets intentionally outside Git

The following must be preserved/restored, never committed:

- `runtime.env` and host-specific bind-source paths;
- TLS CA/certificate/key material;
- all Docker secret files and Control Center Vault material;
- PostgreSQL, MariaDB and any MinIO volume/dump state;
- Control Center catalogs, registered application metadata, backup metadata
  and the admitted hosted-workload runtime lock;
- application `.env` files, uploads, logs, queues and runtime caches;
- provider receipts/capabilities and generated render/build evidence.

Local image IDs and Docker build cache are reproducible artifacts, not source
authorities. A rebuilt image must come from the locked checkout and tracked
Dockerfile.

## Candidate versus effective live render

The raw scalar comparison contains 62 paths:

- 47 are build-context or bind-source locations. They reflect the clean
  candidate checkout path versus the current live checkout/hotfix path. The
  tracked Traefik static file, admin/project routes and both WAF files were
  byte-matched against their active live sources.
- The live middleware uses `referrerPolicy: same-origin`; the candidate keeps
  the stricter canonical `no-referrer`. This is an intentional security
  correction, not missing live state.
- The external `runtime.env` originally resolved the candidate Control Center
  CA mount to the server leaf certificate. It was backed up and corrected to
  `certs/ca.pem`; the regenerated render now matches the running CA source.
- The candidate removes the runtime dependency from Control Center to
  Keycloak because active authentication is PostgreSQL-backed `app-passkey`.
- The candidate narrows first-configuration access from all RFC1918 ranges to
  the actual LAN `192.168.1.0/24` plus loopback.
- The candidate adds bounded backup-queue defaults and the direct backup jobs
  directory already consumed by the current Control Center code.
- The candidate image name is the canonical local build tag instead of the
  ad-hoc live hotfix tag.
- NATS uses the image-absolute `/nats-server`; the live hotfix invokes the same
  binary through `PATH`.

The running Keycloak container still has an obsolete
`platform-first-configuration` theme bind mount. It is absent from the
effective Compose render and is not an intentional V1 dependency. It must be
removed by a controlled Keycloak recreate after the new main is available;
the theme is not imported as source authority.

A scoped security review compared the candidate with every non-dependency file
inside the running Control Center. The exact delta is ten modified files plus
the live-only migration 005. With the observed `app-passkey` mode, direct first
configuration returns before the Keycloak adapter/store/config path, the
handoff route is disabled and `/auth/callback` is not exposed. Migration 006,
not migration 005, owns the active passkey tables. The legacy handoff bytes and
their existing PostgreSQL rows are therefore preserved as external historical
state but are not imported as intentional V1 software.

## Validation completed

- candidate Compose render: PASS, 32 services and exact live service names;
- external hosted-workload lock: present, private and SHA-256 matched to the
  source lock;
- Traefik static config, admin/project routes and WAF mounted files: byte
  match, except the documented safer middleware policy;
- Control Center test suite: 128/128 PASS;
- edge/WAF focused tests: 9/9 PASS;
- Control Center npm audit: 0 advisories;
- Control Center disposable PostgreSQL integration: PASS;
- FeI Platform: PHP syntax 264 files and 722/722 tests PASS;
- Stexor: frozen install, typecheck, 134/134 tests and all builds PASS;
- hosted applications: PHP syntax 167 files plus each app's focused quality,
  smoke or static verifier PASS;
- fresh clean clone/status check for all three application repositories: PASS.

Stexor's existing dependency advisory gate remains red on both its old and
new parity source. Dependency upgrades are post-parity work because changing
them during source capture would no longer reproduce the live application;
newly rebuilt Stexor images must not be promoted as supply-chain clean until
that separate remediation passes.

## Closure conditions

`V1_0_PARITY` becomes PASS only when all of the following are true:

1. this platform candidate is merged through protected-main checks;
2. a fresh checkout of the resulting `main` is clean;
3. that checkout plus the three locked app revisions renders the same 32
   service names and the intentional render differences above only;
4. external CA state still points to the actual CA file;
5. the final commit/tree and validation evidence are recorded here.

Until then, no V1.1 legacy deletion or Cohort 1 deploy is authorized.
